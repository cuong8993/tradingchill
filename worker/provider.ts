import { demoCandles, demoQuote } from './demo';
import type { Env } from './types';
const FINNHUB='https://finnhub.io/api/v1';
async function finnhub<T>(env:Env,path:string,params:Record<string,string|number>):Promise<T>{if(!env.FINNHUB_API_KEY)throw new Error('DEMO');const url=new URL(FINNHUB+path);Object.entries(params).forEach(([k,v])=>url.searchParams.set(k,String(v)));url.searchParams.set('token',env.FINNHUB_API_KEY);const r=await fetch(url);if(r.status===429)throw new Error('Market data rate limit reached.');if(!r.ok)throw new Error(`Market data provider returned ${r.status}.`);return r.json() as Promise<T>}
export async function getQuote(env:Env,symbol:string){if(!env.FINNHUB_API_KEY)return demoQuote(symbol);const r=await finnhub<{c:number;d:number;dp:number;h:number;l:number;o:number;pc:number;t:number}>(env,'/quote',{symbol});if(!r.c)throw new Error(`No quote for ${symbol}.`);return{symbol,price:r.c,change:r.d,changePercent:r.dp,high:r.h,low:r.l,open:r.o,previousClose:r.pc,timestamp:r.t}}
export async function getCandles(env:Env,symbol:string,resolution:string,from:number,to:number){if(!env.FINNHUB_API_KEY)return demoCandles(symbol,resolution,from,to);const r=await finnhub<{s:string;t?:number[];o?:number[];h?:number[];l?:number[];c?:number[];v?:number[]}>(env,'/stock/candle',{symbol,resolution,from,to});if(r.s!=='ok'||!r.t?.length)throw new Error(`No historical data for ${symbol}.`);return r.t.map((time,i)=>({time,open:r.o?.[i]??0,high:r.h?.[i]??0,low:r.l?.[i]??0,close:r.c?.[i]??0,volume:r.v?.[i]??0}))}
export async function searchLive(env:Env,q:string){return finnhub<{result?:Array<{symbol:string;displaySymbol?:string;description?:string;type?:string}>}>(env,'/search',{q})}
