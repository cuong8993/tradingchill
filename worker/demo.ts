import type { Candle, Quote } from './types';
export const DEMO=[['AAPL','Apple'],['MSFT','Microsoft'],['NVDA','NVIDIA'],['AMZN','Amazon'],['GOOGL','Alphabet'],['META','Meta'],['TSLA','Tesla'],['AMD','AMD'],['SPY','S&P 500 ETF'],['QQQ','Nasdaq 100 ETF'],['NFLX','Netflix'],['COIN','Coinbase'],['JPM','JPMorgan'],['PLTR','Palantir']] as const;
function hash(s:string){let h=2166136261;for(const c of s)h=Math.imul(h^c.charCodeAt(0),16777619);return Math.abs(h>>>0)}
export function demoQuote(symbol:string):Quote{const base=30+(hash(symbol)%500),m=Math.floor(Date.now()/60000),phase=(hash(symbol)%999)/91,price=base*(1+Math.sin(m/9+phase)*.008),pc=base*(1+Math.sin((m-390)/17+phase)*.004),change=price-pc;return{symbol,price:+price.toFixed(2),change:+change.toFixed(2),changePercent:+(change/pc*100).toFixed(2),high:+(Math.max(price,pc)*1.006).toFixed(2),low:+(Math.min(price,pc)*.994).toFixed(2),open:+(pc*1.001).toFixed(2),previousClose:+pc.toFixed(2),timestamp:Math.floor(Date.now()/1000)}}
export function demoCandles(symbol:string,resolution:string,from:number,to:number):Candle[]{const seconds=resolution==='D'?86400:resolution==='W'?604800:Math.max(60,Number(resolution||5)*60),start=Math.max(from,to-seconds*700),seed=hash(symbol)%5000,out:Candle[]=[];let prev=30+(hash(symbol)%500),i=0;for(let time=start;time<=to;time+=seconds){const wave=Math.sin((time/seconds+seed)/11)*.004+Math.sin((time/seconds+seed)/37)*.002,open=prev,close=Math.max(.01,open*(1+wave)),spread=Math.abs(Math.sin(i*1.77+seed))*.004+.001;out.push({time,open:+open.toFixed(4),high:+(Math.max(open,close)*(1+spread)).toFixed(4),low:+(Math.min(open,close)*(1-spread)).toFixed(4),close:+close.toFixed(4),volume:Math.round(500000+Math.abs(Math.sin(i/5+seed))*6500000)});prev=close;i++}return out}
